<?xml version="1.0" ?>
<odoo>
  <data>
    <!--Email template -->
    <!-- delete id="account.email_template_edi_invoice" model="mail.template"/ -->
    <record id="email_template_edi_invoice" model="mail.template">
      <field name="name">Invoice - Send by Email</field>
      <field name="email_from">${object.company_id and object.company_id.email or ''}</field>
      <field name="subject">${object.company_id.name} Invoice (Ref ${object.number or 'n/a'})</field>
      <field name="partner_to">${object.partner_id.id}</field>
      <field name="model_id" ref="account.model_account_invoice"/>
      <field name="auto_delete" eval="True"/>
      <field name="report_template" ref="account.account_invoices"/>
      <field name="report_name">Invoice_${(object.number or '').replace('/','_')}_${object.state == 'draft' and 'draft' or ''}</field>
      <field name="lang">${object.partner_id.lang}</field>
      <field name="body_type">qweb</field>
      <field name="body_view_id"
             ref="primewood_mail_management.email_template_edi_invoice"/>"
    </record>

    <!--Default Notification Email template for invoices-->
    <!-- delete id="account.mail_template_data_notification_email_account_invoice"
            model="mail.template"/ -->
    <record id="mail_template_data_notification_email_account_invoice" model="mail.template">
      <field name="name">Primewood Invoice Notification Email</field>
      <field name="subject">${object.subject}</field>
      <field name="model_id" ref="mail.model_mail_message"/>
      <field name="auto_delete" eval="True"/>
      <field name="body_type">qweb</field>
      <field name="body_view_id"
             ref="primewood_mail_management.invoice_notification_email"/>
    </record>
  </data>
</odoo>
